import * as React from 'react';
import { useSpring, animated } from "react-spring";
import './header.css';


const OpenLeaderboardIcon = ({onClick}) => {

    
  };

  export default OpenLeaderboardIcon;
